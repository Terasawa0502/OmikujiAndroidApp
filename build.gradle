// build.gradle (プロジェクトルート)
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2' // Gradleプラグインのバージョン
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}